import ticketstate

def selectplace(number):
    if number == 0:
        return "700 select place complete"+ticketstate.place[number]
    elif number ==1:
        return "700 select place complete"+ticketstate.place[number]
    elif number ==2:
        return "700 select place complete"+ticketstate.place[number]
    elif number ==3:
        return "700 select place complete"+ticketstate.place[number]
    else:
        return "709 Invalid place"

def selectzone(number):
    if number == 0:
        return "700 select place complete"+ticketstate.zone[number]
    elif number ==1:
        return "700 select place complete"+ticketstate.zone[number]
    elif number ==2:
        return "700 select place complete"+ticketstate.zone[number]
    elif number ==3:
        return "700 select place complete"+ticketstate.zone[number]
    else:
        return "709 Invalid zone"